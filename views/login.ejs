<!-- views/login.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #FFF; /* Hvid baggrundsfarve */
      color: #333; /* Sort tekstfarve */
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: url('/billeder/bmarble.webp') center/cover no-repeat;
    }

    h1 {
      text-align: center;
    }

    form {
      background-color: #333; /* Sort baggrundsfarve til form */
      color: #FFF; /* Hvid tekstfarve */
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      max-width: 400px;
      width: 100%;
      margin-top: 20px; /* Afstand mellem overskrift og formular */
    }

    label {
      display: block;
      margin-bottom: 8px;
    }

    input {
      width: 100%;
      padding: 8px;
      margin-bottom: 16px;
      box-sizing: border-box;
    }

    button {
      background-color: #000000; /* Pink knapfarve */
      color: #FFF; /* Hvid tekstfarve */
      padding: 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
    }

    button:hover {
      background-color: #1f1f1f; /* Lysere sort ved hover */
    }

    h1 {
      color: #FFF;
    }

    a {
      text-align: center;
      margin-top: 20px;
      color: #FFF; /* Sort tekstfarve */
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1>Login</h1>
  <form action="/login" method="post">
    <label for="username">Brugernavn:</label>
    <input type="text" id="username" name="username" required>
    <br>
    <label for="password">Adgangskode:</label>
    <input type="password" id="password" name="password" required>
    <br>
    <button type="submit">Login</button>
  </form>
  <a href="/register">Opret bruger</a>
</body>
</html>
